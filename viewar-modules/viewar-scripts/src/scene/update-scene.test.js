import test from 'tape';

import analyzeNodes from './update-scene.js';

test('analyzeNodes()', assert => {
  const msg = '... works';
  
  //===== ASSEMBLE =====
  
  const updateScene = {};
  
  //======= ACT ========
  
  // ...
  
  //====== ASSERT ======
  
  const actual = {};
  const expected = {};
  
  assert.equals(actual, expected, msg);
  
  assert.end();
});
