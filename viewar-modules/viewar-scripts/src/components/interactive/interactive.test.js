import test from 'tape';

import { Interactive } from './interactive.js';

test('Interactive', assert => { 
  const msg = '... can be created';
  
  //===== ASSEMBLE =====
  
  const interactive = {};
  
  //======= ACT ========
  
  // ...
  
  //====== ASSERT ======
  
  const actual = {};
  const expected = {};
  
  assert.equals(actual, expected, msg);
  
  assert.end();
});
